# Manim Teaching Studio 依赖清单
# 核心流水线(制作视频)必需:
manim>=0.18
numpy
Pillow

# 交互式画面预览(preview/server.py)需要:
flask>=3.0

# 一键启动器(preview_launcher.pyw)需要;缺失时自动降级为 tkinter 简化界面:
# PySide6>=6.5

# 其余外部依赖(非 pip):LaTeX(MathTex 公式)、ffmpeg(抽帧/拼接/时长解析,
# 加入 PATH 或设置 FFMPEG_DIR 环境变量)。
