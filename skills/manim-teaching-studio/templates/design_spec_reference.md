# {project_name} - 视频设计书(Design Spec)

> 本文件是人类可读的设计叙事,回答"为什么这样设计"。机器可读的执行契约见 `spec_lock.md`。
> 策略师在九项确认单被用户确认后产出本文件,结构固定为以下十一节,节名不得增删改。

## I. 项目信息

- 主题:{主题中文名 + 英文名}
- slug:{projects 目录名}
- 受众:{如 本科低年级(普物+微积分基础)}
- 使用场景:{如 课程导入 / 期末复习 / 科普}
- 目标时长:{如 180 秒}
- 日期:{YYYY-MM-DD}

## II. 视频规格

- 分辨率:{1920×1080}  帧率:{60fps}
- 渲染档位:低清预览 480×854@15(画面审查用)/ 高清成片 1080p60
- 成片格式:分场景 mp4 + 合并成片 mp4

## III. 视觉主题

- 风格:{如 3b1b 深板岩底}
- 配色表(角色 → HEX,与 spec_lock ## colors 一致):
  - 背景:{#282c34}
  - 主蓝:{#58b9ff}(入射 / A 区)
  - 青:{#8be9fd}(透射 / B 区)
  - 强调黄:{#ffd866}(结论 / 标题)
  - 红:{#ff6188}(势垒 / 经典限制)
  - 绿:{#a6e22e}(透射 / 反射对比)
- 强调色纪律:每帧 ≤3 个强调色,其余白/半透明白
- 透明度层级:副标题 0.55 / 推导中间步骤 0.35 / 区域填充 0.16~0.22 / 背景图形 0.10

## IV. 字体系统

- 英文字体 FONT:{Segoe UI}
- 中文字体 CJK_FONT:{Microsoft YaHei}(备选:SimSun 宋体 / STKaiti 楷体)
- TeX 模板:{默认 LaTeX}
- 字号层级:标题 42 / 英文副标题 28 / 正文 24~28 / 标签 20~24 / 注释 16~18
- 硬规则:公式用 MathTex、中文用 Text(font=CJK_FONT),绝不混用

## V. 语言与字幕

- 画面语言:{中文}  旁白语言:{中文}
- 字幕密度:3.5~5 秒/句
- 术语表:{全篇统一译名,如 tunneling=隧穿,barrier=势垒}

## VI. 公式与数学深度

- 公式清单(必须出现的公式,与 spec_lock ## math_locked 一致):
  - {如 薛定谔方程、透射系数 T 表达式}
- 推导策略:{哪些展开、哪些淡出保留(opacity 0.35)}
- 符号约定:{如 E/V₀ 记号、ħ 使用}

## VII. 类比与数值模拟策略

- 类比清单(每个类比必须附边界分析:映射表 → 优点 → 风险边界 → 正确用法):
  - {类比名}:{一句话边界结论}
- 数值模拟验证清单(凡含时物理过程必做):
  - 方法:{如 split-step Fourier}
  - 关键参数:{如 E/V₀ ≈ 0.73 使三现象并存}
  - 守恒自检:{如 概率归一化、能量守恒}
  - 诚实性约定:{如 透射振幅最小可见下限 0.05,数值显示真实 T}

## VIII. 场景大纲

| 场景 | 目录 | 主题 | 类型 | 时长预算 | 关键画面 |
|---|---|---|---|---|---|
| scene01 | scene01_xxx | {引入} | anchor | {22s} | {现象对比} |
| scene02 | scene02_xxx | {推导} | derivation | {28s} | {公式推导} |

类型:anchor(结构页)/ definition(定义式)/ derivation(推导式)/ demo(演示式)

## IX. 禁区清单

- 物理表述禁区(与 spec_lock ## forbidden 一致):
  - {如 禁止"能量借贷后归还"式误导表述;禁止暗示守恒律瞬时破坏}
- 视觉禁区:
  - {如 禁止硬编码新颜色;禁止 3D 陡峭 Surface(Cairo 伪影)}

## X. 审查与交付

- 科学审查:{五维度全量,P0 清零}
- 画面审查:{按阶段边界抽帧 2~4 帧/场景,豆包视觉 API,A 级清零}
- 交付物:分场景 mp4 + 合并成片 + 全部源码 + 双 spec + 审查报告
- 渲染流程:低清(画面审查)→ 用户确认 → 高清 → 时长校验 → 拼接交付

## XI. 技术约束提醒

- 场景代码必须过 `manim_lint.py`(0 error)
- 每场景写前重读 `spec_lock.md`,颜色/字体/公式必须来自该文件
- run_time 全显式,总时长受 shot_table.md 预算约束
- Windows 路径含空格时引号包裹
